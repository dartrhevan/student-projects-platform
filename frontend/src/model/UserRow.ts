export default class UserRow {
    constructor(public name: string, public surname: string, public roles: string, public skills: string,
                public username: string, public project: string, public interests: string) {
    }
}
