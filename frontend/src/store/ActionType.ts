export enum ActionType {
    Login,
    Logout,
    OpenMainMenu,
    CloseMainMenu,
    // OpenProjectMenu,
    // CloseProjectMenu,
    SetPage,
    SetPageSize,
    InitPaging,
    OpenDialog,
    CloseDialog
}
